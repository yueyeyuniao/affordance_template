# Necessary configuration information for a robot end effector configuration pose (mapping to the robot .srdf stored poses)

# the name of the end effector group this pose info applies
string group

# the name of the pose
string name

# the abstract template id this end-effector pose maps to
uint8 id
