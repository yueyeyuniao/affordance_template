# info for scaling display objects

# the template class type 
string class_type

# the id of the template whos object we will scale
uint8 id

# the object to scale 
string object_name

# the scale factor
float64 scale_factor

# the end_effector adjustment scale factor
float64 end_effector_scale_factor


